Database folder for the backend service.

This folder contains the TypeORM database configuration used by the
backend application. It is used for development and testing. The
`pharmacy_test` database is used for integration tests.

Do not include production secrets in this file. Use environment variables
to configure host, port, user, and password.
